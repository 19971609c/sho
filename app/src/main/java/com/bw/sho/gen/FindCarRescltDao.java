package com.bw.sho.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bw.sho.bean.FindCarResclt;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "FIND_CAR_RESCLT".
*/
public class FindCarRescltDao extends AbstractDao<FindCarResclt, Void> {

    public static final String TABLENAME = "FIND_CAR_RESCLT";

    /**
     * Properties of entity FindCarResclt.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property CommodityId = new Property(0, int.class, "commodityId", false, "COMMODITY_ID");
        public final static Property CommodityName = new Property(1, String.class, "commodityName", false, "COMMODITY_NAME");
        public final static Property Count = new Property(2, int.class, "count", false, "COUNT");
        public final static Property Pic = new Property(3, String.class, "pic", false, "PIC");
        public final static Property Price = new Property(4, int.class, "price", false, "PRICE");
        public final static Property IsCheck = new Property(5, boolean.class, "isCheck", false, "IS_CHECK");
    };


    public FindCarRescltDao(DaoConfig config) {
        super(config);
    }
    
    public FindCarRescltDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"FIND_CAR_RESCLT\" (" + //
                "\"COMMODITY_ID\" INTEGER NOT NULL ," + // 0: commodityId
                "\"COMMODITY_NAME\" TEXT," + // 1: commodityName
                "\"COUNT\" INTEGER NOT NULL ," + // 2: count
                "\"PIC\" TEXT," + // 3: pic
                "\"PRICE\" INTEGER NOT NULL ," + // 4: price
                "\"IS_CHECK\" INTEGER NOT NULL );"); // 5: isCheck
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"FIND_CAR_RESCLT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, FindCarResclt entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCommodityId());
 
        String commodityName = entity.getCommodityName();
        if (commodityName != null) {
            stmt.bindString(2, commodityName);
        }
        stmt.bindLong(3, entity.getCount());
 
        String pic = entity.getPic();
        if (pic != null) {
            stmt.bindString(4, pic);
        }
        stmt.bindLong(5, entity.getPrice());
        stmt.bindLong(6, entity.getIsCheck() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, FindCarResclt entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCommodityId());
 
        String commodityName = entity.getCommodityName();
        if (commodityName != null) {
            stmt.bindString(2, commodityName);
        }
        stmt.bindLong(3, entity.getCount());
 
        String pic = entity.getPic();
        if (pic != null) {
            stmt.bindString(4, pic);
        }
        stmt.bindLong(5, entity.getPrice());
        stmt.bindLong(6, entity.getIsCheck() ? 1L: 0L);
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public FindCarResclt readEntity(Cursor cursor, int offset) {
        FindCarResclt entity = new FindCarResclt( //
            cursor.getInt(offset + 0), // commodityId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // commodityName
            cursor.getInt(offset + 2), // count
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // pic
            cursor.getInt(offset + 4), // price
            cursor.getShort(offset + 5) != 0 // isCheck
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, FindCarResclt entity, int offset) {
        entity.setCommodityId(cursor.getInt(offset + 0));
        entity.setCommodityName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCount(cursor.getInt(offset + 2));
        entity.setPic(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setPrice(cursor.getInt(offset + 4));
        entity.setIsCheck(cursor.getShort(offset + 5) != 0);
     }
    
    @Override
    protected final Void updateKeyAfterInsert(FindCarResclt entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(FindCarResclt entity) {
        return null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
